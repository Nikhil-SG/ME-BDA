#include <stdio.h>
/*
void swap (int *a, int *b){
    int temp = *a;
    *a = *b;
    *b = temp;
}

void sortAscending(int arr[], int n){
    for(int i=0;i<n-1; i++){
        for(int j=0;j<n-i-1;j++){
            if(arr[j]>arr[j+1]){
                swap(&arr[j],&arr[j+1]);
            }
        }
    }
}

void sortDescending(int arr[], int n){
    for(int i=0;i<n-1;i++){
        for(int j=0;j<n-i-1;j++){
            if(arr[j]<arr[j+1]){
                swap(&arr[j],&arr[j+1]);
            }
        }
    }
}

int main(){
    int arr[] = {64,35,68,97,15,68,15,78};
    int n = sizeof(arr) / sizeof(arr[0]);

    printf("Original Array: ");
    for(int i = 0; i<n;i++){
        printf("%d ",arr[i]);
    }
    printf("\n");

    sortAscending(arr,n);
    printf("Array in Ascending Order: ");
    for (int i=0;i<n;i++){
        printf("%d ",arr[i]);
    }
    printf("\n");

    sortDescending(arr,n);
    printf("Array in Descending Order: ");
    for (int i=0; i<n;i++){
        printf("%d ",arr[i]);
    }
    printf("\n");
    return 0;
}
*/
