#include<stdio.h>
#include<stdlib.h>
/*
int main()
{

    FILE *file;
    file = fopen("output.txt","w");

    if (file==NULL)
    {
        printf("No file exist. \n");
        return 1;
    }

    char data[] = " ";
    fprintf(file,"%s",data);

    fclose(file);

    file = fopen("output.txt","r");
    if (file==NULL)
    {
        printf("file is empty.\n");
        return 1;
    }

    char str[100];
    while(!feof(file))
    {
        fgets(str, 99, file);
        printf(str);
    }
    return 0;
}*/

